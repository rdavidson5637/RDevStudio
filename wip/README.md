# wip

Unfinished work parked out of the build.

`tsconfig.json` excludes this folder, so nothing in here is type-checked or
bundled. Move a file back into `components/` or `lib/` when it is ready.

## guitar-lab

The Guitar Lab stub. It renders scale notes, key signature and Roman numerals
but has no fretboard yet. It needs two packages that are not installed and not
in `package.json`:

- `@rdavidson/theory`
- `nuqs`

`/guitar-lab` currently serves a coming-soon page instead.
